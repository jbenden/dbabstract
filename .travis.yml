# Mac OS X (https://github.com/travis-ci-tester/toolchain-table)
#
language:
  - cpp
services:
  - mysql
  - sqlite
before_install:
  - apt-get mysql-dev sqlite-dev libpoco-dev
  - mysql -e 'create database test;'
install: LIBPATH="/usr/local/lib" ./configure --prefix=/usr/local --enable-mysql=/usr --enable-sqlite=/usr
script:
  - make
  - make install
  - /usr/local/bin/test_db
